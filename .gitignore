node_modules/
.expo/
dist/

# Build output and tool logs. The Expo export bundle in particular inlines
# every EXPO_PUBLIC_* value at build time, so it must never be committed.
.expo-export-check/
*.log
npm-install-output.txt
tsc-out.txt

*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
coverage/
.DS_Store
.env
.env.local
.env.*.local
/android/.gradle
/android/build
/android/.cxx

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Native projects are generated by `expo prebuild` from app.json on every EAS
# build, so they are not committed. Editing them by hand does not survive.
/android/
/ios/
